# {{@@ env['dotdrop_warning'] @@}}
# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono, Icons 14
font pango: Droid Sans Mono, FontAwesome, MFixx_custom 12

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
#bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return exec --no-startup-id xterm -u8

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop
bindsym $mod+d exec --no-startup-id j4-dmenu-desktop --use-xdg-de --display-binary --dmenu='dmenu -h 25 -i -nb "#262626" -nf "#ababab" -sb "#000000" -sf "#ffffff" -fn "Droid Sans Mono-12"'

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+odiaeresis focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+odiaeresis move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# move workspace to another output
bindsym Ctrl+$mod+Left move workspace to output left
bindsym Ctrl+$mod+Down move workspace to output down
bindsym Ctrl+$mod+Up move workspace to output up
bindsym Ctrl+$mod+Right move workspace to output right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+F11 fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+a layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
#bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# custom workspaces
set $ws_chromium 
set $ws_im 
set $ws_mail 
set $ws_files 
set $ws_gimp 6:
set $ws_ff 
set $ws_pycharm 
set $ws_subl 
set $ws_mixxx MIXXX

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number 1; workspace number 1
bindsym $mod+Shift+2 move container to workspace number 2; workspace number 2
bindsym $mod+Shift+3 move container to workspace number 3; workspace number 3
bindsym $mod+Shift+4 move container to workspace number 4; workspace number 4
bindsym $mod+Shift+5 move container to workspace number 5; workspace number 5
bindsym $mod+Shift+6 move container to workspace number 6; workspace number 6
bindsym $mod+Shift+7 move container to workspace number 7; workspace number 7
bindsym $mod+Shift+8 move container to workspace number 8; workspace number 8
bindsym $mod+Shift+9 move container to workspace number 9; workspace number 9
bindsym $mod+Shift+0 move container to workspace number 10; workspace number 10

# reload the configuration file
bindsym $mod+Shift+e reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
#bindsym $mod+Shift+r restart
bindsym $mod+Shift+r exec --no-startup-id ~/.i3/i3restart.sh
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# Variables
set $scrot scrot -m '%Y-%m-%d_%H-%M-%S_$wx$h.png' -e 'mv $f ~/Pictures/screenshots/'
set $sink `pactl list short sinks | grep RUNNING | cut -f1`

# set $raiseorlaunch ~/Linux/git/raiseorlaunch/raiseorlaunch/__main__.py
set $raiseorlaunch raiseorlaunch

bindsym $mod+ctrl+l exec --no-startup-id ~/.i3/i3lock.sh
bindsym XF86PowerOff exec --no-startup-id  ~/.i3/i3exit.py # needs polkit installed
bindsym Ctrl+Mod1+BackSpace exec --no-startup-id  ~/.i3/i3exit.py # needs polkit installed
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume {{@@ env['audio_device'] @@}} +4db
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume {{@@ env['audio_device'] @@}} -4db
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute {{@@ env['audio_device'] @@}} toggle
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 2 toggle
bindsym XF86MonBrightnessDown exec --no-startup-id xbacklight -dec 10
bindsym XF86MonBrightnessUp exec --no-startup-id xbacklight -inc 10
bindsym XF86Sleep exec --no-startup-id systemctl suspend
bindsym XF86HomePage exec --no-startup-id systemctl suspend
bindsym $mod+F12 exec --no-startup-id $raiseorlaunch -r -c SpeedCrunch
bindsym XF86Explorer exec --no-startup-id $raiseorlaunch -r -c SpeedCrunch
bindsym XF86Calculator exec --no-startup-id $raiseorlaunch -r -c SpeedCrunch
bindsym Print exec --no-startup-id $scrot  # All screens
bindsym Mod1+Sys_Req exec --no-startup-id $scrot -u  # Focused window
bindsym --release Ctrl+Print exec --no-startup-id $scrot -s  # Select rectangle

bindsym $mod+f exec --no-startup-id $raiseorlaunch -w $ws_ff -c Firefox -s Navigator
bindsym $mod+Mod1+f exec --no-startup-id firefox
bindsym $mod+shift+f move container to workspace $ws_ff; workspace $ws_ff
bindsym $mod+c exec --no-startup-id $raiseorlaunch -w $ws_chromium -c Chromium -e 'i3-msg exec chromium'
bindsym $mod+Mod1+c exec --no-startup-id chromium
bindsym $mod+shift+c move container to workspace $ws_chromium; workspace $ws_chromium
bindsym XF86Mail exec --no-startup-id $raiseorlaunch -w $ws_mail -c Thunderbird -s Mail
bindsym $mod+t exec --no-startup-id $raiseorlaunch -w $ws_mail -c Thunderbird -s Mail
bindsym $mod+shift+t move container to workspace $ws_mail; workspace $ws_mail
bindsym $mod+n exec --no-startup-id $raiseorlaunch -w $ws_files -c Nemo -s nemo -e nemo
bindsym $mod+Mod1+n exec --no-startup-id nemo
bindsym $mod+shift+n move container to workspace $ws_files; workspace $ws_files
bindsym $mod+s exec --no-startup-id $raiseorlaunch -w $ws_subl -c Sublime_text -s sublime_text -e subl
bindsym $mod+Mod1+s exec --no-startup-id subl
bindsym $mod+shift+s move container to workspace $ws_subl; workspace $ws_subl
bindsym $mod+period exec --no-startup-id $raiseorlaunch -r -c Scribes -e 'scribes ~/Notes.txt'

bindsym $mod+u exec --no-startup-id i3-input -F 'exec --no-startup-id i3-msg "workspace FF" && firefox -new-tab %s' -P 'URL: '
bindsym $mod+g exec --no-startup-id i3-input -F 'exec --no-startup-id i3-msg "workspace FF" && firefox https://www.google.com/search?q="%s"' -P 'Searchterm: '
bindsym $mod+Next workspace next
bindsym $mod+Prior workspace prev
bindsym $mod+Tab workspace back_and_forth
bindsym $mod+Shift+Tab move container to workspace back_and_forth; workspace back_and_forth
bindsym $mod+x [urgent=latest] focus
bindsym $mod+Mod1+Left focus output left
bindsym $mod+Mod1+Right focus output right
bindsym $mod+Shift+period sticky toggle

{%@@ if env['intellij'] == "true" @@%}
bindsym $mod+p exec --no-startup-id $raiseorlaunch -w $ws_pycharm -c jetbrains-pycharm -e pycharm
bindsym $mod+Mod1+p exec --no-startup-id pycharm
bindsym $mod+shift+p move container to workspace $ws_pycharm; workspace $ws_pycharm
{%@@ endif @@%}

{%@@ if env['multiple_keylayouts'] == "true" @@%}
bindsym $mod+y exec --no-startup-id ~/scripts/keylayouttoggle.sh
bindsym $mod+Greek_zeta exec --no-startup-id ~/scripts/keylayouttoggle.sh
{%@@ endif @@%}

{%@@ if profile == "fuckup" @@%}
bindsym $mod+m exec --no-startup-id $raiseorlaunch -w $ws_mixxx -c Mixxx
bindsym $mod+shift+m move container to workspace $ws_mixxx; workspace $ws_mixxx
bindsym $mod+i workspace $ws_im
bindsym $mod+shift+i move container to workspace $ws_im; workspace $ws_im
{%@@ elif profile == "ant" @@%}
bindsym $mod+i exec --no-startup-id $raiseorlaunch -w $ws_im -c Rambox
{%@@ endif @@%}

#bindsym $mod+c exec --no-startup-id $raiseorlaunch -r -c XTerm -e "xterm -e /bin/bash -l -c 'cmus'"
#bindsym $mod+n exec --no-startup-id $raiseorlaunch -w $ws_files -c Nemo -s nemo -e "dbus-launch nemo"
#bindsym $mod+p exec --no-startup-id i3-input -F 'rename workspace to "%s"' -P 'New name: '

# mouse bindings
# The middle button over a titlebar kills the window
bindsym button2 kill
# The middle button and a modifer over any part of the window kills the window
bindsym --whole-window $mod+button2 kill
# The right button toggles floating
#bindsym button3 floating toggle
bindsym $mod+button3 floating toggle

# floating rules
for_window [class="I3exit" instance="i3exit"] floating enable
for_window [class="Qjackctl"] floating enable
for_window [class="Skype" window_role="ConversationsWindow"] floating enable
for_window [class="Pidgin" window_role="conversation"] floating enable
for_window [class="Gimp"] floating enable
for_window [class="Main.py" instance="guake"] floating enable
for_window [class="GCompris"] floating enable
for_window [title="KeePassX - Auto-Type"] floating enable
for_window [class="Arandr"] floating enable
for_window [class="Firefox" instance="Browser"] floating enable
for_window [title="Preferences$"] floating enable
for_window [title="Options$"] floating enable
for_window [class="Nm-connection-editor"] floating enable
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="About"] floating enable
for_window [class="Lxappearance"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Virt-manager"] floating enable
for_window [class="Transmission-gtk"] floating enable
for_window [instance="TeamViewer.exe"] floating enable
for_window [instance="pdwindow" class="PdWindow"] floating enable
for_window [class="PatchWindow"] floating enable
for_window [class="Hedgewars" instance="hedgewars" title="Hedgewars"] floating enable
for_window [class="hwengine"] floating enable
for_window [class="SuperTuxKart"] floating enable
for_window [class="Qjoypad"] floating enable
for_window [class="Phoenix" title="Game Library"] floating enable
for_window [class="Phoenix" title="Configuration Settings"] floating enable
for_window [class="Phoenix" title="Load Image"] floating enable
for_window [class="feh"] floating enable
for_window [class="Scribes"] floating enable
for_window [class="Viewnior"] floating enable
for_window [class="SpeedCrunch"] floating enable
for_window [class="Xterm" title="cmus"] floating enable
for_window [class="Wine" instance="LPD8 Editor.exe"] floating enable
for_window [class="Elicit"] floating enable
for_window [class="Thunderbird" title="Edit"] floating enable
for_window [class="Thunderbird" title="Write"] floating enable
for_window [class="Thunderbird" instance="Calendar"] floating enable
for_window [class="California"] floating enable
for_window [class="Virt-manager" title="Virtual Machine"] floating enable
for_window [class="M64py"] floating enable
for_window [class="Blueman-manager"] floating enable
for_window [class="Gtklp"] floating enable
for_window [class="Cheese"] floating enable
for_window [class="Guvcview" instance="guvcview"] floating enable
for_window [title="GEM"] floating enable
for_window [title="Clementine image viewer"] floating enable
for_window [class="Pidgin" title="File Transfers"] floating enable
for_window [class="Pinentry"] floating enable
for_window [class="Firefox" title="Install user style"] floating enable
for_window [title="Developer Tools - "] floating disable
#for_window [class="Skype" window_role="ConversationsWindow"] sticky enable

# Damn IntelliJ
for_window [class="jetbrains-pycharm"] focus_on_window_activation none
no_focus [class="jetbrains-pycharm"]

# borders
for_window [class="(?i)xterm"] border none
for_window [class="Atom"] border none
for_window [class="Nemo"] border none
for_window [title="padctl - Mapping"] border none
for_window [class="Scribes"] border none
for_window [class="Viewnior"] border 1pixel
for_window [class="Speedcrunch"] border none
for_window [class="I3exit" instance="i3exit"] border 1pixel

# transparency
for_window [class="Scribes"] exec --no-startup-id transset-df -a 0.9
for_window [class="Speedcrunch"] exec --no-startup-id transset-df -a 0.94
for_window [class="I3exit" instance="i3exit"] exec --no-startup-id transset-df -a 0.9

# Border toggle
bindsym $mod+b border toggle

# force to workspace and (in some cases, if it works) directly focus new windows
assign [class="Thunderbird"] $ws_mail
for_window [class="Thunderbird"] focus
for_window [class="Atom"] focus
for_window [class="Firefox"] focus
assign [class="Skype"] $ws_im
for_window [class="Skype"] focus
assign [class="Pidgin"] $ws_im
assign [class="Rambox"] $ws_im
for_window [class="Rambox"] focus
assign [class="Gimp"] $ws_gimp
for_window [class="Gimp"] focus

# Scratchpad
# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad
# Show the first scratchpad window
bindsym $mod+minus scratchpad show
# Show the sup-mail scratchpad window, if any.
# bindsym mod4+s [title="^Sup ::"] scratchpad show

# Autostart always
exec_always --no-startup-id guake && xdotool key F12 # run guake and hide it.

# Autostart
exec --no-startup-id ~/Linux/git/streamwall/src/streamwall/streamwall.sh -tbn
exec --no-startup-id nm-applet
exec --no-startup-id keepassx
exec --no-startup-id xmodmap -e "pointer = 3 2 1"
exec --no-startup-id nemo -n
#exec --no-startup-id conky -c ~/.conky/conkyrc
exec --no-startup-id xcompmgr -c -n # real transparency (this needs xcompmgr to be installed)
exec --no-startup-id xset b off
exec --no-startup-id xset s off    # disable screen blanking
exec --no-startup-id xset -dpms    # disable screen blanking
exec --no-startup-id unclutter {{@@ env['unclutter_args'] @@}}

{%@@ if env['has_dropbox'] == "true" @@%}
exec --no-startup-id dropbox
{%@@ endif @@%}

#exec --no-startup-id dispwin ~/Linux/thinkpad/colour_profile/from_internet/ThinkPad_T450s_FHD_LP140WF3_SPD1_01.icm
#exec --no-startup-id blueman-applet
#exec --no-startup-id owncloud
exec --no-startup-id i3-msg workspace 1

# custom config
workspace_auto_back_and_forth yes # switch to a workspace and with the same key back
focus_follows_mouse no

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
  position top
  status_command py3status --dbus-notify -c ~/.i3/i3status.conf
  workspace_buttons yes
  strip_workspace_numbers no
  tray_output primary
  {%@@ if profile == "fuckup" @@%}
  tray_padding 1
  {%@@ endif @@%}
  bindsym button4 none
  bindsym button5 none
  colors {
    separator #666666
    background #000000
    statusline #ffffff
    ## colorclass <border> <background> <text>
    #focused_workspace #4c7899 #285577 #ffffff
    focused_workspace #333333 #5d5d5d #ffffff
    active_workspace #333333 #5f676a #ffffff
    inactive_workspace #333333 #222222 #888888
    urgent_workspace #2f343a #900000 #ffffff
  }
}

## colorclass <border> <background> <text> <indicator>
client.focused #000000 #000000 #ffffff #000000
client.focused_inactive #3e3e3e #3e3e3e #ffffff #484e50
client.unfocused #181818 #181818 #888888 #292d2e
client.urgent #2f343a #900000 #ffffff #900000
